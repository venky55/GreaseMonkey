<div class="page-header">
  <h1><%= @listing.title %></a><br>
  <small><%= @listing.area %>,<%= @listing.city%></small><br>
  <small><%= @listing.workinghours %></small><br>
  <small><%= @listing.workingdays %></small><br>
  <small><%= @listing.phonenumber %></small><br>
  <small><%= @listing.address %></small><br>
  <small><%= @listing.url %></small><br>
  </h1>
</div>
<%if @listing.tags%>
<!-- <div><%= render @listing.tags %></div> -->
<%end%>


<% if @listing.user == current_user -%>
	<div class="btn-group">
		<%= link_to 'Edit', edit_listing_path(@listing), class: "btn btn-default" %>
		<%= link_to 'Destroy', @listing, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-default" %>
	</div>
<% end %>